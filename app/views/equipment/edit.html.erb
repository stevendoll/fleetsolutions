<% content_for(:title) { "Edit Equipment" } %>

  <div class="hgroup">
    <h1>Edit Equipment</h1>
    <ul class="breadcrumb pull-right">
      <li><a href="<%= root_path %>">Home</a> <span class="divider">/</span></li>
      <li><a href="<%= opportunity_equipment_index_path(@equipment.opportunity) %>">Equipment</a> <span class="divider">/</span></li>
      <li class="active">Edit Equipment</li>
    </ul>
  </div>


<%= render 'form' %>

<%= link_to 'Show', [@equipment.opportunity, @equipment] %> |
<%= link_to 'Back', @equipment.opportunity %>
